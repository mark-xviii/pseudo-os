import Icon from '../../classes/icon.class'

import * as walking_walkman_image from '../../assets/images/walking_walkman.gif'

const iconsMock: Icon[] = []

iconsMock.push({
  id: 'walking_walkman',
  alias: 'walking_walkman.gif',
  imageURL: walking_walkman_image.default,
  x: null,
  y: null,
} as Icon)

iconsMock.push({
  id: 'walking_walkman2',
  alias: 'walking_walkman.gif',
  imageURL: walking_walkman_image.default,
  x: null,
  y: null,
} as Icon)

export default iconsMock
